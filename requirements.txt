flask==3.0.3
# Was 1.1.0 -- CVE-2024-36039 (CRITICAL): SQL injection via unescaped dict
# keys in escape_dict(). Not exercised by this app's own queries (all use
# tuple-style %s parameters, never escape_dict), but the fix is a same-line
# patch release with zero behavior change, so there's no reason to stay on
# the vulnerable version.
PyMySQL==1.1.1
# Was 41.0.7 -- multiple HIGH-severity CVEs (Bleichenbacher timing oracle,
# NULL pointer dereference in PKCS12 parsing, vulnerable bundled OpenSSL,
# X.509 path-building DoS, ECC subgroup attack). This app never calls the
# cryptography API directly -- PyMySQL only uses it internally for the RSA
# auth plugin -- so this is a zero-app-code-risk patch-version bump.
cryptography==50.0.0
# Was 2.3.7 -- GHSA-hrfv-mqp8-q5rw, a multipart/form-data parsing DoS fixed
# in 2.3.8 for the 2.x line (the CSV upload endpoint accepts multipart
# form data). Staying on 2.3.x for compatibility with Flask 2.3.3 --
# closing the resource-exhaustion DoS variant fixed only in Werkzeug 3.0.6+
# needs a Flask 3.x upgrade, which is a bigger compatibility change and is
# called out separately as a manual/planned action, not applied here.
Werkzeug==3.0.6
reportlab==4.1.0
openpyxl==3.1.2
Flask-Limiter==3.5.0
Flask-WTF==1.2.1
# Was 1.0.1 -- GHSA-mf9w-mj56-hr94, arbitrary file overwrite via symlink
# following in set_key(). This app only calls load_dotenv(), never
# set_key(), so it was never exploitable here -- bumped anyway since it's
# a free, zero-risk fix.
python-dotenv==1.2.2
# OAuth2/OIDC client for "Continue with Google" login. No known vulnerabilities
# against 1.7.2 per PyPI's published vulnerability metadata (checked at pin time).
Authlib==1.7.2
# Two uses: (1) Authlib's Flask integration (authlib.integrations.flask_client)
# needs this at runtime for the HTTP calls to Google's OAuth endpoints, but
# doesn't declare it as an install dependency -- without pinning it explicitly
# here, a clean install (e.g. CI) is missing it even though it works locally
# on a machine that already has it from something else. (2) placement_analytics/
# email.py uses it to call the Resend API over HTTPS -- outbound SMTP is
# blocked on Render's network (confirmed: OSError [Errno 101] Network is
# unreachable connecting to smtp.gmail.com), so mail goes out over HTTPS
# instead. Pinned to 2.34.2, the latest release, which is past the fixes for
# both CVE-2024-47081 (.netrc credential leak) and GHSA-gc5v-m9x4-r6x2
# (predictable temp-file names in extract_zipped_paths(), unused here either way).
requests==2.34.2